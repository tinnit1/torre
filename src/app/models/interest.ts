export class Interest {
  constructor(public id: string,
              public code: number,
              public name: string,
              public media: any[],
              public created: Date) {
  }
}
